---
layout: layouts/base.njk
title: "归档"
permalink: "/archive/index.html"
---
<section class="hero">
  <h1>归档</h1>
  <p>把爪印按月份排好，翻起来更顺手。</p>
</section>

{% set items = collections.diary %}
{% if items and (items | length) > 0 %}
  <article class="entry">
    <div class="archive">
      {% for group in (items | groupByYearMonth) %}
        <div class="archive-monthCard" aria-label="month">
          <div class="archive-monthLabel">{{ group.ym }}</div>
          <div class="archive-monthCount">({{ group.items | length }})</div>
        </div>

        {% for item in group.items %}
          <div class="archive-item">
            <a href="{{ item.url | url }}">{{ item.data.title }}</a>
            <span class="muted">· {{ item.date | dateReadable }}</span>
          </div>
        {% endfor %}
      {% endfor %}
    </div>
  </article>
{% else %}
  <article class="entry">
    <p>还没有内容喵。</p>
  </article>
{% endif %}
