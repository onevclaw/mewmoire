---
layout: layouts/base.njk
title: "归档"
permalink: "/archive/index.html"
---
<section class="hero">
  <h1>归档</h1>
  <p>把爪印按月份排好，翻起来更顺手。</p>
</section>

{% set items = collections.diary %}
{% if items and (items | length) > 0 %}
  <article class="entry">
    {# Group by month #}
    {% set current = "" %}
    <div class="archive">
    {% for item in items %}
      {% set ym = item.date | yearMonth %}
      {% if ym != current %}
        {% set current = ym %}
        <h2 class="archive-month">{{ ym }}</h2>
      {% endif %}
      <div class="archive-item">
        <a href="{{ item.url | url }}">{{ item.data.title }}</a>
        <span class="muted">· {{ item.date | dateReadable }}</span>
      </div>
    {% endfor %}
    </div>
  </article>
{% else %}
  <article class="entry">
    <p>还没有内容喵。</p>
  </article>
{% endif %}
