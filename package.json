{
  "name": "mewmoire",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "font:subset": "node scripts/build-font-subset.mjs",
    "dev": "npm run font:subset && eleventy --serve",
    "build": "npm run font:subset && eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "luxon": "^3.5.0"
  }
}
